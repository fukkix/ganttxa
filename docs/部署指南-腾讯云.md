# GanttXa 腾讯云部署指南（简化版）

**日期**: 2026-04-13  
**适用**: 腾讯云服务器 + Docker

---

## 🎯 部署方案

使用 **Docker Compose + Nginx** 实现多项目部署：

```
腾讯云服务器
├── Nginx (反向代理)
│   ├── ganttxa.yourdomain.com → GanttXa (端口 8080)
│   ├── project2.yourdomain.com → 项目2 (端口 8081)
│   └── project3.yourdomain.com → 项目3 (端口 8082)
└── Docker 容器
    ├── GanttXa (前端 + 后端 + 数据库)
    ├── 项目2 容器
    └── 项目3 容器
```

---

## 📋 准备工作

### 1. 服务器要求

- 操作系统: Ubuntu 20.04+ / CentOS 7+
- 内存: 至少 2GB
- 磁盘: 至少 20GB
- 已安装 Docker 和 Docker Compose

### 2. 域名配置（可选）

如果使用域名访问：
- 在腾讯云 DNS 添加 A 记录
- `ganttxa.yourdomain.com` → 服务器 IP
- `api.ganttxa.yourdomain.com` → 服务器 IP

---

## 🚀 快速部署（5 步）

### 步骤 1: 连接服务器并安装软件

```bash
# 连接服务器
ssh root@your-server-ip

# 安装 Docker
curl -fsSL https://get.docker.com | sh

# 安装 Docker Compose
apt install docker-compose -y

# 安装 Nginx
apt install nginx -y

# 启动 Docker
systemctl start docker
systemctl enable docker
```

---

### 步骤 2: 克隆项目到服务器

```bash
# 创建项目目录
mkdir -p /opt/projects
cd /opt/projects

# 克隆项目
git clone https://github.com/fukkix/ganttxa.git
cd ganttxa
```

---

### 步骤 3: 配置环境变量

```bash
# 复制环境变量模板
cp .env.prod.example .env.prod

# 编辑环境变量
nano .env.prod
```

**修改以下内容**:

```bash
# 数据库密码（必须修改）
DB_PASSWORD=your_secure_password_123

# JWT 密钥（必须修改，至少 32 个字符）
JWT_SECRET=your_jwt_secret_key_at_least_32_chars_long

# 如果使用域名
CORS_ORIGIN=https://ganttxa.yourdomain.com
VITE_API_URL=https://api.ganttxa.yourdomain.com

# 如果使用 IP + 端口
# CORS_ORIGIN=http://your-server-ip:8080
# VITE_API_URL=http://your-server-ip:3000
```

保存并退出（Ctrl+X, Y, Enter）

---

### 步骤 4: 构建和启动服务

```bash
# 构建 Docker 镜像
docker-compose -f docker-compose.prod.yml build

# 启动服务
docker-compose -f docker-compose.prod.yml --env-file .env.prod up -d

# 查看服务状态
docker-compose -f docker-compose.prod.yml ps

# 查看日志
docker-compose -f docker-compose.prod.yml logs -f
```

**等待 1-2 分钟让服务完全启动**

---

### 步骤 5: 配置 Nginx 反向代理

#### 方案 A: 使用域名（推荐）

```bash
# 创建 Nginx 配置
nano /etc/nginx/sites-available/ganttxa
```

**粘贴以下内容**:

```nginx
# 前端
server {
    listen 80;
    server_name ganttxa.yourdomain.com;

    location / {
        proxy_pass http://localhost:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

# 后端 API
server {
    listen 80;
    server_name api.ganttxa.yourdomain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }
}
```

**启用配置**:

```bash
# 创建软链接
ln -s /etc/nginx/sites-available/ganttxa /etc/nginx/sites-enabled/

# 测试配置
nginx -t

# 重启 Nginx
systemctl restart nginx
```

#### 方案 B: 使用 IP + 端口（简单）

如果不使用域名，直接通过 IP 访问：

- 前端: `http://your-server-ip:8080`
- 后端: `http://your-server-ip:3000`

**配置防火墙**:

```bash
# 允许端口
ufw allow 8080/tcp
ufw allow 3000/tcp
ufw allow 22/tcp
ufw enable
```

---

## ✅ 验证部署

### 1. 检查服务状态

```bash
# 查看容器
docker ps

# 应该看到 3 个容器运行中:
# - ganttxa-frontend
# - ganttxa-backend
# - ganttxa-postgres
```

### 2. 测试访问

**使用域名**:
- 前端: https://ganttxa.yourdomain.com
- 后端: https://api.ganttxa.yourdomain.com

**使用 IP**:
- 前端: http://your-server-ip:8080
- 后端: http://your-server-ip:3000

### 3. 测试功能

1. 打开前端页面
2. 注册/登录账号
3. 创建项目
4. 添加任务
5. 测试实时协作（打开两个浏览器窗口）

---

## 🔄 更新部署

当代码有更新时：

```bash
cd /opt/projects/ganttxa

# 使用自动部署脚本
./deploy.sh

# 或者手动执行
git pull origin main
docker-compose -f docker-compose.prod.yml build
docker-compose -f docker-compose.prod.yml up -d
```

---

## 🔒 配置 HTTPS（推荐）

### 使用 Let's Encrypt 免费证书

```bash
# 安装 Certbot
apt install certbot python3-certbot-nginx -y

# 申请证书
certbot --nginx -d ganttxa.yourdomain.com -d api.ganttxa.yourdomain.com

# 按提示输入邮箱并同意条款
# Certbot 会自动配置 HTTPS
```

**自动续期**:

```bash
# 测试续期
certbot renew --dry-run

# 添加定时任务（每天检查）
echo "0 0 * * * certbot renew --quiet" | crontab -
```

---

## 🎯 多项目部署

### 部署其他项目

```bash
# 创建项目目录
cd /opt/projects
mkdir project2
cd project2

# 克隆项目
git clone https://github.com/your/project2.git .

# 修改 docker-compose.yml 端口
# 前端: 8081
# 后端: 3001

# 启动服务
docker-compose up -d
```

### 添加 Nginx 配置

```bash
nano /etc/nginx/sites-available/project2
```

```nginx
server {
    listen 80;
    server_name project2.yourdomain.com;

    location / {
        proxy_pass http://localhost:8081;
        # ... 其他配置
    }
}
```

```bash
ln -s /etc/nginx/sites-available/project2 /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx
```

---

## 📊 常用命令

### 查看日志

```bash
# 查看所有日志
docker-compose -f docker-compose.prod.yml logs -f

# 查看特定服务日志
docker-compose -f docker-compose.prod.yml logs -f backend
docker-compose -f docker-compose.prod.yml logs -f frontend
```

### 重启服务

```bash
# 重启所有服务
docker-compose -f docker-compose.prod.yml restart

# 重启单个服务
docker-compose -f docker-compose.prod.yml restart backend
```

### 停止服务

```bash
# 停止所有服务
docker-compose -f docker-compose.prod.yml down

# 停止并删除数据
docker-compose -f docker-compose.prod.yml down -v
```

### 进入容器

```bash
# 进入后端容器
docker exec -it ganttxa-backend sh

# 进入数据库容器
docker exec -it ganttxa-postgres psql -U ganttxa_user -d ganttxa
```

---

## 🔍 故障排查

### 问题 1: 容器无法启动

```bash
# 查看详细日志
docker-compose -f docker-compose.prod.yml logs

# 检查端口占用
netstat -tulpn | grep :8080
netstat -tulpn | grep :3000
```

### 问题 2: 无法访问

```bash
# 检查防火墙
ufw status

# 检查 Nginx 状态
systemctl status nginx

# 检查 Nginx 日志
tail -f /var/log/nginx/error.log
```

### 问题 3: 数据库连接失败

```bash
# 检查数据库容器
docker logs ganttxa-postgres

# 测试数据库连接
docker exec -it ganttxa-postgres psql -U ganttxa_user -d ganttxa
```

---

## 📝 部署检查清单

- [ ] 服务器已安装 Docker 和 Docker Compose
- [ ] 已克隆项目代码
- [ ] 已配置 .env.prod 文件
- [ ] 已构建 Docker 镜像
- [ ] 已启动所有容器
- [ ] 已配置 Nginx（如果使用域名）
- [ ] 已配置防火墙
- [ ] 已测试前端访问
- [ ] 已测试后端 API
- [ ] 已配置 HTTPS（可选）

---

## 🎉 完成！

现在你的 GanttXa 已经成功部署到腾讯云服务器！

**访问地址**:
- 前端: https://ganttxa.yourdomain.com
- 后端: https://api.ganttxa.yourdomain.com

或者（使用 IP）:
- 前端: http://your-server-ip:8080
- 后端: http://your-server-ip:3000

---

## 📞 需要帮助？

查看详细文档:
- [完整部署指南](DEPLOYMENT-GUIDE-CLOUD.md)
- [部署检查清单](DEPLOYMENT-CHECKLIST.md)

---

**部署指南版本**: 1.0  
**最后更新**: 2026-04-13

